// The template lives in template.js so the server can load it without a JSX
// loader. This file only exists so `npm run email` still discovers it.
export { default, PREVIEW_DATA } from "./template.js";
